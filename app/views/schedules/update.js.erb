$('.calendar').fullCalendar('removeSchedules', [<%= @schedule.id %>]);
$('.calendar').fullCalendar(
  'renderSchedule',
  $.parseJSON("<%=j render(@schedule, format: :json).html_safe %>"),
  true
);
$('.modal').modal('hide');
